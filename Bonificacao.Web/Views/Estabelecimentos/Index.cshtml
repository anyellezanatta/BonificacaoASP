@model IEnumerable<Bonificacao.Data.Estabelecimento>
@{ ViewBag.Title = "Estabelecimentos"; }

<div class="row">
    <div class="col-lg-12">
        <div class="ibox">
            <div class="ibox-content">
                <h2>Lista de estabelecimentos</h2>
                <small>Aqui você pode gerenciar os estabelecimentos do sistema</small>
                <div class="padding-top">
                    <p>
                        @Html.ActionLink("Cadastrar", "Cadastrar")
                    </p>
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>@Html.DisplayNameFor(model => model.Nome)</th>
                                    <th>@Html.DisplayNameFor(model => model.GrupoEstabelecimento.Nome)</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var it in Model)
                                {
                                    <tr>
                                        <td>@Html.DisplayFor(modelItem => it.Nome)</td>
                                        <td>@Html.DisplayFor(modelItem => it.GrupoEstabelecimento.Nome)</td>
                                        <td>
                                            @Html.ActionLink("Editar", "Editar", new { id = it.Id }) | @Helpers.Delete("Estabelecimentos", "Deletar", it.Id)
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
